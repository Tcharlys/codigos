#include <stdio.h>
#include <locale.h>

/* 6. (Prova EST/2010-1) Escreva um programa em C que contenha uma função chamada tamanho que calcula o
número de caracteres de uma palavra digitada pelo usuário. O usuário poderá digitar uma palavra com no
máximo 20 caracteres. O programa deverá, usando a função tamanho, imprimir o número de caracteres da
palavra digitada pelo usuário. */

int main()
{
    setlocale(LC_ALL, " ");


    getchar();
    return (0);
}